---
- name: awx test playbook
  hosts: ansible-node1
  become: yes    
  tasks:
    - name: Run demo task on pinging servers only
      ping:
    - name: debug task
      debug:
        msg: "{{ ansible_facts }}" 
