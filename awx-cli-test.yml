---
- name: awx test playbook
  hosts: eoc-node1
  become: yes    
  tasks:
    - name: Run demo task on pinging servers only
      ping:
    - name: debug task
      debug:
        msg: "{{ ansible_facts }}" 
